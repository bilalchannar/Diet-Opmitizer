services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/

  mongo:
    image: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
